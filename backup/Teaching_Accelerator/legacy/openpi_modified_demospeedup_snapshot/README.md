# OpenPI DemoSpeedup Snapshot

This directory preserves the old in-place OpenPI prototype before cleaning
`openpi_modified`.

Created on 2026-06-22.

## Contents

- `modified_files/`: current copies of tracked OpenPI files that had local
  DemoSpeedup/difficulty-label integration edits.
- `untracked_files/`: untracked `demo_difficulty` source files and tests that
  were living inside `openpi_modified`.

## Status

This is a legacy snapshot for reference only. The active standalone prototype is
the top-level `Teaching_Accelerator` package and its docs:

- `scripts/compute_teaching_labels.py`
- `teaching_accelerator/`
- `docs/openpi_integration_plan.md`

Keep future DemoSpeedup-style work here instead of editing `openpi_modified`
directly.
